{
  "name": "zali-subgraph",
  "version": "1.0.0",
  "description": "The Graph subgraph for Zali trivia game on Base",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --studio zali-trivia",
    "create-local": "graph create --node http://localhost:8020/ zali-trivia",
    "remove-local": "graph remove --node http://localhost:8020/ zali-trivia",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 zali-trivia",
    "test": "graph test"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "^0.56.0",
    "@graphprotocol/graph-ts": "^0.31.0"
  },
  "devDependencies": {
    "matchstick-as": "^0.5.0"
  }
}
